# Appendix Summary v1

This appendix is a support package, not an extension of the claim. It keeps the main paper narrow by moving secondary sensitivity, auxiliary comparator material, local robustness checks, provenance, and scope notes out of the core narrative.

The appendix contains seven pieces only:

- dense-friction sensitivity as diagnostic support for the same implementation-side story
- CC-TA-LBIP as auxiliary evidence under the same-state, fixed-forecast-map setup
- a narrow `c`-ablation as local auxiliary robustness only
- a compact target-versus-executed support table showing that the positive-cost interpretation changes with the evaluation object
- the `Similar forecasting information, different realized decision quality` table, kept as conservative metric-level support rather than a main-text finding
- a concise provenance note stating how the dense-friction appendix figure was preserved under the documented setup
- a short scope summary that records submission discipline without adding new claims

The main paper should remain fully interpretable without this appendix. If readers skip the appendix entirely, they should still see the forecasting problem statement, the single main empirical result, and the accounting-based evaluation argument from the main text alone.
