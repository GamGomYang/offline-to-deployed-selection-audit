As auxiliary supporting evidence rather than a main identification result, CC-TA-LBIP preserves the same 918-dimensional state used in the RL package and keeps the fixed ridge forecast map (`ridge_alpha=30`, `fit_passes=2`) while tuning only the cost-scaling constant `c` on validation. Under the documented validation rule, the selected comparator remains `c=3000`. The `kappa=0` row preserves the collapse logic: the selected arm and the `c=0` baseline have identical executed-path net Sharpe (`1.1875`) and identical executed turnover (`1.29032`), so there is no free zero-cost smoothing. At positive cost, the same-state comparator moves in the same direction as the RL implementation story, with executed-path net Sharpe improving from `0.0661` to `1.0199` at `kappa=5e-4` and from `-1.0529` to `0.9679` at `kappa=1e-3`, while executed turnover falls from `1.29032` to `0.11894` and `0.06333`. This package remains supporting evidence only and does not replace the frozen-policy RL main result.
