This dense-friction package was regenerated under the locked protocol from preserved downstream manuscript and figure inputs, not as a source-artifact reproduction. On the dense diagnostic grid `kappa={2e-4, 5e-4, 1e-3, 2e-3}`, the locked selected-point interpretation remains centered on `eta=0.5`, and its paired-median Delta Sharpe rises from `+0.0041` to `+0.0409` as frictions increase. The best-interior line steepens in the same direction, from `+0.0113` to `+0.0761`, but it remains diagnostic only; it is included to show that the execution frontier becomes more consequential as costs rise, not to replace the selected-point main result.
