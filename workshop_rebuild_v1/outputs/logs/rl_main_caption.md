Table X. Held-out selected-point comparison under a frozen learned policy on the effective `2024-02-14` to `2025-12-31` window. The validation-selected interior point `eta=0.5` is compared only against the immediate-execution baseline `eta=1.0` on `kappa={0, 5e-4, 1e-3}` using executed-path net Sharpe as the headline metric and `TOexec` as supporting evidence. Positive-cost gains appear alongside a large executed-turnover reduction, while the zero-cost row remains near-flat.
