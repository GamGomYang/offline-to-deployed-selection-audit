Target-path quantities are reported for diagnosis only and do not replace executed-path evaluation as the primary accounting object. At the validation-selected operating point `eta=0.5`, target turnover is about twice realized executed turnover across the locked cost rows (`TOtgt/TOexec ≈ 2.00`), while tracking remains small but nonzero at about `0.00259`. The final path gap then grows with cost, from `0.00067` at `kappa=0` to `0.00369` at `kappa=5e-4` and `0.00739` at `kappa=1e-3`, which supports an implementation-side accounting interpretation rather than a stronger-alpha story.
