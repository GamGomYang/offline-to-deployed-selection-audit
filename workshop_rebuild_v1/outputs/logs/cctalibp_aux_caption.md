Table X. Auxiliary supporting evidence from a linear forecast-plus-convex-optimization comparator. CC-TA-LBIP preserves the same 918-dimensional state and the same executed-path accounting as the RL analysis while keeping the fixed ridge forecast map and tuning only the cost-scaling constant `c` on validation, which remains aligned at `c=3000`. The `kappa=0` row preserves the documented collapse to the `c=0` baseline, while the positive-cost rows show same-direction implementation-side gains. This comparator stays auxiliary and does not supersede the paper's main frozen-policy RL result.
