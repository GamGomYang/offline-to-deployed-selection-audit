The results section should remain centered on the frozen-policy RL selected-point comparison. Under a frozen learned policy, the validation-selected interior point `eta=0.5` improves held-out executed-path net Sharpe relative to the immediate-execution baseline `eta=1.0` by `+0.0105` at `kappa=5e-4` and `+0.0213` at `kappa=1e-3`, while reducing median executed turnover from `0.02200` to `0.01095`. The `kappa=0` row remains near-flat at `-0.0002`, so the core result should still be written as an implementation-side gain that appears under frictions rather than as evidence of a stronger alpha signal.

The next paragraph should explain the mechanism without displacing that main result. Target-path quantities remain diagnostic only: at `eta=0.5`, target turnover is about twice realized executed turnover across the locked cost rows (`TOtgt/TOexec ≈ 2.00`), tracking remains small but nonzero at about `0.00259`, and the final path gap grows with cost from `0.00067` at `kappa=0` to `0.00739` at `kappa=1e-3`. This keeps the accounting story aligned with the main claim by showing why realized-path evaluation is primary and why target-based quantities should not become headline metrics.

The dense-friction package should then appear as a **regenerated canonical diagnostic bundle under the locked protocol**, not as source-artifact reproduction. In the clean rebuild lane, that bundle is now the archived dense-friction source package for the workshop build. Its locked selected-point interpretation remains centered on `eta=0.5`, and its paired-median Delta Sharpe rises from `+0.0041` to `+0.0409` as frictions increase on the dense diagnostic grid `kappa={2e-4, 5e-4, 1e-3, 2e-3}`. The best-interior line steepens in the same direction, but it remains diagnostic only and cannot replace the selected-point main result.

CC-TA-LBIP should remain auxiliary. Using the same 918-dimensional state and the same executed-path accounting while keeping the ridge forecast map fixed, the validation-selected comparator remains `c=3000`, collapses to the `c=0` baseline at `kappa=0`, and shows same-direction positive-cost improvements outside RL. The reviewer-facing robustness note can now state that a narrow held-out ablation over `c in {0, 2000, 3000, 4000}` is locally stable around the selected operating point without changing the locked validation rule: `c=3000` remains the archived operating point, `c=4000` qualifies but is not selected, and `c=2000` falls below the validation threshold. This strengthens the auxiliary package in a reviewer-safe way without turning it into a second identification result.

The same-forecast table can remain in the main text only as supporting analysis under a conservative title. In v2 form, the contrast is more explicit: at `kappa=5e-4` and `1e-3`, forecast MSE changes by only `-0.03%` and `+0.28%`, while executed turnover and realized cost fall by `90.8%` and `95.1%`, and net Sharpe rises by `+0.9539` and `+2.0208`. That contrast supports main-text supporting use under `Similar forecasting information, different realized decision quality`, but it still does not justify an exact-same-forecast claim because previous executed weights remain part of the state.
