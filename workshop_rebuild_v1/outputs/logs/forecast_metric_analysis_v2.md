Forecast metrics in this package are still computed before the decision layer from the fixed fitted ridge forecast map, while decision metrics are computed after the decision layer from the executed portfolio path. The comparison remains restricted to `c=0` versus `c=3000`, and the wording remains conservative: this package supports `similar forecasting information`, not an exact-same-forecast claim.

Main-text judgment: this remains appropriate for **main-text supporting use** under the conservative title `Similar forecasting information, different realized decision quality`. It should not be upgraded to the stronger `Same forecast, different realized decision quality` title because exact forecast identity has not been directly established and previous executed weights remain part of the state.

The v2 contrast is now explicit on a change scale. At `kappa=5e-4`, forecast MSE changes by only `-0.03%`, sign accuracy by `+0.41` percentage points, and rank IC by `+0.0038`, while executed turnover and realized cost both fall by `90.8%` and net Sharpe rises by `+0.9539`. At `kappa=1e-3`, forecast MSE changes by only `+0.28%`, sign accuracy by `+0.49` percentage points, and rank IC by `+0.0033`, while executed turnover and realized cost both fall by `95.1%` and net Sharpe rises by `+2.0208`.

This is the key reading: forecast-side movement stays small on the held-out comparison, while decision-side movement is large and economically material in the positive-cost rows. The `kappa=0` row still collapses at the displayed precision, which preserves the zero-cost logic. Under the documented workshop criterion that forecast-side differences should be small relative to decision-side differences, the package continues to pass as main-text supporting evidence. If a stricter standard demanded direct proof of identical forecast vectors across arms, this package should move to the appendix rather than be overclaimed.
