Figure X and Table X. Diagnostic-only target-vs-executed accounting summaries for the validation-selected operating point `eta=0.5` on `kappa={0, 5e-4, 1e-3}`. Executed-path evaluation remains primary; target-path quantities are included only to diagnose how partial execution changes realized trading and path accounting. Target turnover stays about twice executed turnover, tracking remains small but nonzero, and the final equity gap widens as transaction costs rise, so this package supports the RL main result rather than replacing it.
