Using CC-TA-LBIP only, we keep the fitted ridge forecast map fixed and compare `c=0` with the validation-selected `c=3000`, computing forecast metrics before the decision layer and realized portfolio metrics after the decision layer. Because previous executed weights remain inside the state, we describe the two arms as having similar rather than exactly identical forecasting information. In the positive-cost rows, the forecast-side changes remain small on the displayed scale: forecast MSE moves by only `-0.03%` at `kappa=5e-4` and `+0.28%` at `kappa=1e-3`, while sign accuracy moves by only `+0.41` and `+0.49` percentage points. Over the same rows, decision-side changes are much larger: executed turnover and realized cost fall by `90.8%` and `95.1%`, and net Sharpe rises by `+0.9539` and `+2.0208`. The `kappa=0` row still collapses, so this v2 table remains suitable for main-text supporting use under the conservative `Similar forecasting information` framing, but it should not be used to claim exact forecast identity.
